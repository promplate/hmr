[project]
name = "mcp-demo"
description = "Demo MCP server for testing mcp-hmr"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mcp",
]

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"

[tool.uv.sources]
mcp-hmr = { workspace = true }